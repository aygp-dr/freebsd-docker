# FreeBSD Docker Configuration
# Copy to .env and customize

# VM Resources
MEMORY=2G
CPUS=2
DISK_SIZE=10G

# Networking
NETWORK_MODE=user        # user, bridge, or none
ENABLE_BRIDGE=false      # Set to true for bridge networking
SSH_PORT=22

# Optional Features
ENABLE_VNC=false         # Enable VNC access
VNC_PORT=5900
ZFS_DISK=                # Size for additional ZFS disk (e.g., "5G")

# FreeBSD Version
FREEBSD_VERSION=14.3-RELEASE

# Jail Configuration
JAIL_ROOT=/usr/local/jails
JAIL_DATASET=zroot/jails
BASE_RELEASE=14.0-RELEASE